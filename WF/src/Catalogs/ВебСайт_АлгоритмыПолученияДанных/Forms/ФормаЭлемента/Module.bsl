
&НаКлиенте
Процедура НаименованиеПриИзменении(Элемент)	
	Объект.Наименование = СтрЗаменить(Объект.Наименование, " ", "");
	Если Не ЗначениеЗаполнено(Объект.ИмяФайлаJSON) Тогда
		Объект.ИмяФайлаJSON = Объект.Наименование + ".json";		
	КонецЕсли;	
КонецПроцедуры
